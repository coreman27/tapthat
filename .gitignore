# Dependencies
node_modules/

# Capacitor build output / native project artifacts
www/
android/
# Note: ios/ IS committed (has customized Info.plist + generated icons);
# its own ios/.gitignore excludes Pods, build, and DerivedData.

# App Store screenshot captures (regenerate from the Simulator)
store-screenshots/

# macOS
.DS_Store

# Logs
*.log
npm-debug.log*
