{
  "name": "tapthat",
  "version": "1.0.0",
  "private": true,
  "description": "DON'T TAP THAT — a reflex/psychological tap game for iPhone (web core + Capacitor native wrapper).",
  "scripts": {
    "icons": "node scripts/generate-icons.js",
    "www": "node scripts/sync-www.js",
    "serve": "node scripts/serve.js",
    "ios:add": "npm run www && npx cap add ios",
    "sync": "npm run www && npx cap sync",
    "ios": "npm run www && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/haptics": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
